<p id="notice"><%= notice %></p>

<h1>Moldy Peppers</h1>

<table class="table table-striped col-md-12" id="games">
  <thead>
    <tr>
      <thName</th>
      <th>Type</th>
      <th>Description</th>
      <th>Developer</th>
    </tr>
  </thead>
  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= game.name %></td>
        <td><%= game.name %></td>
        <td><%= game.name %></td>
        <td><%= game.name %></td>
        <td><%= link_to 'Show', game %></td>
        <td><%= link_to 'Edit', edit_game_path(game) %></td>
        <td><%= link_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Game', new_game_path %>
